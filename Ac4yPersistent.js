class Ac4yPersistent extends Ac4y 
{ 
    constructor(object) 
    {
        super(object);
        this.id = id;

        this.gUID = gUID;

        this.owner = owner;

        this.createdAt = createdAt;

        this.updatedAt = updatedAt;

        this.ac4yIdentification = ac4yIdentification;
    }
} // Ac4yPersistent
